datasource sqlite {
  url      = "file:dev.sqlite"
  provider = "sqlite"
}

generator photonjs {
  provider = "photonjs"
  output = "../generated/photon"
}

model Todo {
  id     Int    @id
  body   String @unique
  status String @default("off")
}