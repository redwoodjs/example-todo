# Migration `20190720132201-initial-database`

This migration has been generated by Peter Pistorius at 7/20/2019, 1:22:01 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."User"("id" INTEGER NOT NULL DEFAULT 0 ,"email" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Invoice"("id" INTEGER NOT NULL DEFAULT 0 ,"body" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"User" INTEGER NOT NULL  REFERENCES User(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "file:dev"."User.email._UNIQUE" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190720132201-initial-database
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,22 @@
+datasource sqlite {
+  url      = "file:dev.db"
+  provider = "sqlite"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+model User {
+  id                Int               @id
+  email             String            @unique
+  invoices          Invoice[]
+}
+
+model Invoice {
+  id                Int               @id
+  User              User
+  body              String
+  createdAt         DateTime          @default(now())
+  updatedAt         DateTime          @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190720132201-initial-database)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190720132201-initial-database'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
