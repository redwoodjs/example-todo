# Migration `20190821171556-add-todos`

This migration has been generated by Tom Preston-Werner at 8/21/2019, 5:15:56 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."Todo"("id" INTEGER NOT NULL DEFAULT 0 ,"body" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "file:dev"."Todo.body._UNIQUE" ON "Todo"("body")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190821171556-add-todos
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,13 @@
+datasource sqlite {
+  url      = "file:dev.sqlite"
+  provider = "sqlite"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+model Todo {
+  id   Int    @id
+  body String @unique
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190821171556-add-todos)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190821171556-add-todos'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
