datasource sqlite {
  url      = "file:dev.sqlite"
  provider = "sqlite"
}

generator photonjs {
  provider = "photonjs"
}

model Todo {
  id     Int    @id
  body   String @unique
  status String @default("off")
}