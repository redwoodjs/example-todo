import Check from './Check'

export const on = () => {
  return <Check type="on" />
}

export const off = () => {
  return <Check type="off" />
}

export const plus = () => {
  return <Check type="plus" />
}

export const loading = () => {
  return <Check type="loading" />
}

export default { title: 'Components/Check' }
